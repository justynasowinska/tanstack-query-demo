# TanStack Query Demo

This repo is a short demo of selected `useQuery` use cases with TanStack Query.

## Run locally

1. Install dependencies:

```bash
yarn
```

2. Start the app in development mode:

```bash
yarn dev
```

3. Open the URL shown in the terminal (default: `http://localhost:5173`).
